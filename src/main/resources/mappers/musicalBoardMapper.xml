<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.care.root.mybatis.reservation.musicalBoardMapper">
   	<insert id="mWriteSave" >
		insert into musicalBoard(write_no, title, type, comname, mu_date, mu_place, content, image_file_name, form)
		values(mvc_board_seq.nextval,#{title},#{type},#{comname},#{mu_date},#{mu_place},#{content},#{image_file_name},#{form})
	</insert>
</mapper>